import { useEffect, useMemo, useState } from "react";
import { WorkDrawer } from "../../components/work-drawer";
import { StatusBadge } from "../../components/status-badge";
import { shortDate } from "../../lib/format";
import { addCorrespondenceDocument, createCorrespondence, getMailroomInbox, getMailroomReferenceData, linkCorrespondenceDocument, transitionCorrespondence } from "./repository";
import { MailroomWorkDrawer } from "./mailroom-work-drawer";
import { filterAndSortMailroomItems } from "./workflow";
import type { CorrespondenceDueState, MailroomInboxItem, MailroomReferenceData } from "./types";

type AnyRow = Record<string, any> & { id: string }; type SortMode = "newest" | "oldest" | "due";
const correspondenceTypes = ["eob", "denial_letter", "appeal", "reconsideration", "recoupment_notice", "refund_request", "credentialing_letter", "medical_record_request", "prior_authorization_notice", "payer_correspondence", "general_correspondence"] as const;
const statuses = ["new", "reviewed", "action_required", "in_progress", "pending", "resolved", "closed"] as const;
function humanize(value: string) { return value.replaceAll("_", " ").replace(/\b\w/g, (letter) => letter.toUpperCase()); }
function personName(row?: AnyRow | null) { return row ? [row.first_name, row.last_name].filter(Boolean).join(" ") || "—" : "—"; }
function emptyForm() { return { subject: "", receivedDate: new Date().toISOString().slice(0, 10), correspondenceType: "payer_correspondence", payerId: "", clientId: "", claimId: "", providerId: "", authorizationId: "", appealId: "", dueDate: "", notes: "", assignedUserId: "", documentId: "", requiresAction: false }; }

export function MailroomPage() {
  const [rows, setRows] = useState<MailroomInboxItem[]>([]); const [referenceData, setReferenceData] = useState<MailroomReferenceData | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [showNew, setShowNew] = useState(false); const [saving, setSaving] = useState(false); const [form, setForm] = useState(emptyForm); const [file, setFile] = useState<File | null>(null); const [selectedId, setSelectedId] = useState<string | null>(null);
  const [search, setSearch] = useState(""); const [status, setStatus] = useState("all"); const [type, setType] = useState("all"); const [payer, setPayer] = useState("all"); const [assignment, setAssignment] = useState("all"); const [due, setDue] = useState<"all" | CorrespondenceDueState>("all"); const [sort, setSort] = useState<SortMode>("newest");
  async function load() { setLoading(true); setError(null); try { const [items, refs] = await Promise.all([getMailroomInbox(), getMailroomReferenceData()]); setRows(items); setReferenceData(refs); } catch (err) { setError(err instanceof Error ? err.message : "Unable to load Mailroom."); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  const filtered = useMemo(() => filterAndSortMailroomItems(rows, { search, status: status === "all" ? undefined : status, correspondenceType: type === "all" ? undefined : type, payerName: payer === "all" ? undefined : payer, assignedUserId: assignment === "all" ? undefined : assignment === "unassigned" ? null : assignment, dueState: due === "all" ? undefined : due, sort }), [rows, search, status, type, payer, assignment, due, sort]);
  const selectedIndex = selectedId ? filtered.findIndex((row) => row.id === selectedId) : -1; const selected = selectedIndex >= 0 ? filtered[selectedIndex] : null;
  const clients = (referenceData?.clients ?? []) as AnyRow[]; const providers = (referenceData?.providers ?? []) as AnyRow[]; const payers = (referenceData?.payers ?? []) as AnyRow[]; const claims = (referenceData?.claims ?? []) as AnyRow[]; const authorizations = (referenceData?.authorizations ?? []) as AnyRow[]; const appeals = (referenceData?.appeals ?? []) as AnyRow[]; const documents = (referenceData?.documents ?? []) as AnyRow[];
  const formClaims = form.clientId ? claims.filter((claim) => String(claim.client_id ?? "") === form.clientId) : claims; const formAuthorizations = form.clientId ? authorizations.filter((authorization) => String(authorization.client_id ?? "") === form.clientId) : authorizations; const clientClaimIds = new Set(formClaims.map((claim) => String(claim.id))); const formAppeals = form.claimId ? appeals.filter((appeal) => String(appeal.claim_id ?? "") === form.claimId) : form.clientId ? appeals.filter((appeal) => clientClaimIds.has(String(appeal.claim_id ?? ""))) : appeals; const formDocuments = form.clientId ? documents.filter((document) => !document.client_id || String(document.client_id) === form.clientId) : documents;
  const newDirty = useMemo(() => JSON.stringify(form) !== JSON.stringify(emptyForm()) || Boolean(file), [form, file]);
  async function saveCorrespondence() { if (!form.subject.trim()) { setError("Subject is required."); return; } if (file && form.documentId) { setError("Choose either an existing document or a new file, not both."); return; } setSaving(true); setError(null); let created = false; try { let correspondence = await createCorrespondence({ subject: form.subject.trim(), receivedDate: form.receivedDate, correspondenceType: form.correspondenceType, payerId: form.payerId || null, clientId: form.clientId || null, claimId: form.claimId || null, providerId: form.providerId || null, authorizationId: form.authorizationId || null, appealId: form.appealId || null, assignedUserId: form.assignedUserId || null, dueDate: form.dueDate || null, notes: form.notes.trim() || null }); created = true; if (form.documentId) correspondence = await linkCorrespondenceDocument(correspondence.id, form.documentId); else if (file) correspondence = await addCorrespondenceDocument(correspondence, file); if (form.requiresAction) correspondence = await transitionCorrespondence(correspondence.id, "require_action", "Action required when correspondence was added."); setShowNew(false); setForm(emptyForm()); setFile(null); await load(); setSelectedId(correspondence.id); } catch (err) { const detail = err instanceof Error ? err.message : "Unknown error."; setError(created ? `The correspondence was created, but follow-up setup did not finish. ${detail}` : `Unable to create correspondence. ${detail}`); } finally { setSaving(false); } }
  return <>
    <div className="thera-page-header split"><div><div className="thera-eyebrow">CORRESPONDENCE OPERATIONS</div><h1>Mailroom</h1><p>Receive, classify, link, route, and work payer and administrative correspondence.</p></div><button type="button" className="thera-action" onClick={() => { setError(null); setShowNew(true); }}>+ Add Correspondence</button></div>
    <section className="thera-card" style={{ marginBottom: 16 }}><div className="thera-filter-row"><input className="thera-input" value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Subject, patient, claim, payer, or provider" /><select className="thera-select" value={status} onChange={(e) => setStatus(e.target.value)}><option value="all">All statuses</option>{statuses.map((value) => <option key={value} value={value}>{humanize(value)}</option>)}</select><select className="thera-select" value={type} onChange={(e) => setType(e.target.value)}><option value="all">All correspondence</option>{correspondenceTypes.map((value) => <option key={value} value={value}>{humanize(value)}</option>)}</select><select className="thera-select" value={payer} onChange={(e) => setPayer(e.target.value)}><option value="all">All payers</option>{[...new Set(rows.map((row) => row.payerName).filter((name) => name !== "—"))].sort().map((name) => <option key={name} value={name}>{name}</option>)}</select><select className="thera-select" value={assignment} onChange={(e) => setAssignment(e.target.value)}><option value="all">All assignments</option><option value="unassigned">Unassigned</option>{referenceData?.assignees.map((item) => <option key={item.user_id} value={item.user_id}>{item.display_label}</option>)}</select><select className="thera-select" value={due} onChange={(e) => setDue(e.target.value as typeof due)}><option value="all">All due dates</option><option value="overdue">Overdue</option><option value="due_soon">Due soon</option><option value="current">Current</option><option value="none">No due date</option></select><select className="thera-select" value={sort} onChange={(e) => setSort(e.target.value as SortMode)}><option value="newest">Newest first</option><option value="oldest">Oldest first</option><option value="due">Due date</option></select></div></section>
    {error && <div className="thera-state error" style={{ marginBottom: 12 }}>{error}</div>}
    <section className="thera-card">{loading && <div className="thera-state">Loading Mailroom...</div>}{!loading && filtered.length === 0 && <div className="thera-empty">No correspondence matches these filters.</div>}{!loading && filtered.length > 0 && <div className="thera-table-wrap"><table className="thera-table"><thead><tr><th>Received</th><th>Subject</th><th>Type</th><th>Payer</th><th>Patient</th><th>Claim</th><th>Provider</th><th>Due</th><th>Assignee</th><th>Status</th><th>Action</th><th></th></tr></thead><tbody>{filtered.map((row) => <tr key={row.id} onClick={() => setSelectedId(row.id)} style={{ cursor: "pointer" }}><td>{shortDate(row.receivedDate)}</td><td><strong>{row.subject}</strong></td><td>{humanize(row.correspondenceType)}</td><td>{row.payerName}</td><td>{row.patientName}</td><td>{row.claimNumber}</td><td>{row.providerName}</td><td>{row.dueDate ? shortDate(row.dueDate) : "—"}<div className="thera-table-subtext">{row.dueState === "none" ? "" : humanize(row.dueState)}</div></td><td>{row.assigneeName}</td><td><StatusBadge value={String(row.status)} /></td><td>{row.activeWork ? <><StatusBadge value={String(row.activeWork.priority ?? "normal")} /><div className="thera-table-subtext">Follow-up active</div></> : row.status === "action_required" ? "Follow-up required" : "—"}</td><td><button type="button" className="thera-action secondary" onClick={(e) => { e.stopPropagation(); setSelectedId(row.id); }}>Open</button></td></tr>)}</tbody></table></div>}</section>
    <MailroomWorkDrawer open={Boolean(selected)} onOpenChange={(open) => { if (!open) setSelectedId(null); }} row={selected} refs={referenceData} queuePosition={selectedIndex >= 0 ? `${selectedIndex + 1} of ${filtered.length}` : undefined} previousDisabled={selectedIndex <= 0} nextDisabled={selectedIndex < 0 || selectedIndex >= filtered.length - 1} onPrevious={() => { if (selectedIndex > 0) setSelectedId(filtered[selectedIndex - 1].id); }} onNext={() => { if (selectedIndex >= 0 && selectedIndex < filtered.length - 1) setSelectedId(filtered[selectedIndex + 1].id); }} onSaved={load} />
    {showNew && referenceData && <WorkDrawer open={showNew} onOpenChange={setShowNew} dirty={newDirty} title="Add Correspondence" subtitle="Link the correspondence to the operational records it affects." footer={<div className="thera-filter-row" style={{ justifyContent: "space-between" }}><button type="button" className="thera-action secondary" onClick={() => setShowNew(false)}>Cancel</button><button type="button" className="thera-action" disabled={saving || !form.subject.trim()} onClick={() => void saveCorrespondence()}>{saving ? "Saving..." : "Save Correspondence"}</button></div>}><div className="thera-form-grid"><Field label="Subject"><input className="thera-input" value={form.subject} onChange={(e) => setForm({ ...form, subject: e.target.value })} /></Field><Field label="Received Date"><input className="thera-input" type="date" value={form.receivedDate} onChange={(e) => setForm({ ...form, receivedDate: e.target.value })} /></Field><Field label="Type"><select className="thera-select" value={form.correspondenceType} onChange={(e) => setForm({ ...form, correspondenceType: e.target.value })}>{correspondenceTypes.map((value) => <option key={value} value={value}>{humanize(value)}</option>)}</select></Field><Field label="Payer"><select className="thera-select" value={form.payerId} onChange={(e) => setForm({ ...form, payerId: e.target.value })}><option value="">No payer</option>{payers.map((row) => <option key={row.id} value={row.id}>{row.name}</option>)}</select></Field><Field label="Patient"><select className="thera-select" value={form.clientId} onChange={(e) => setForm({ ...form, clientId: e.target.value, claimId: "", authorizationId: "", appealId: "" })}><option value="">No patient</option>{clients.map((row) => <option key={row.id} value={row.id}>{personName(row)}</option>)}</select></Field><Field label="Claim"><select className="thera-select" value={form.claimId} onChange={(e) => setForm({ ...form, claimId: e.target.value, appealId: "" })}><option value="">No claim</option>{formClaims.map((row) => <option key={row.id} value={row.id}>{row.patient_control_number || row.payer_claim_number || "Claim"}</option>)}</select></Field><Field label="Provider"><select className="thera-select" value={form.providerId} onChange={(e) => setForm({ ...form, providerId: e.target.value })}><option value="">No provider</option>{providers.map((row) => <option key={row.id} value={row.id}>{personName(row)}{row.credentials ? `, ${row.credentials}` : ""}</option>)}</select></Field><Field label="Authorization"><select className="thera-select" value={form.authorizationId} onChange={(e) => setForm({ ...form, authorizationId: e.target.value })}><option value="">No authorization</option>{formAuthorizations.map((row) => <option key={row.id} value={row.id}>{row.authorization_number || "Authorization"}</option>)}</select></Field><Field label="Appeal"><select className="thera-select" value={form.appealId} onChange={(e) => setForm({ ...form, appealId: e.target.value })}><option value="">No appeal</option>{formAppeals.map((row) => <option key={row.id} value={row.id}>{humanize(String(row.appeal_level || "appeal"))} · {humanize(String(row.appeal_status || "unknown"))}</option>)}</select></Field><Field label="Due Date"><input className="thera-input" type="date" value={form.dueDate} onChange={(e) => setForm({ ...form, dueDate: e.target.value })} /></Field><Field label="Assignee"><select className="thera-select" value={form.assignedUserId} onChange={(e) => setForm({ ...form, assignedUserId: e.target.value })}><option value="">Unassigned</option>{referenceData.assignees.map((row) => <option key={row.user_id} value={row.user_id}>{row.display_label}</option>)}</select></Field><Field label="Existing Document"><select className="thera-select" value={form.documentId} onChange={(e) => setForm({ ...form, documentId: e.target.value })}><option value="">No existing document</option>{formDocuments.map((row) => <option key={row.id} value={row.id}>{row.file_name || "Document"}</option>)}</select></Field><Field label="Upload New Document"><input className="thera-input" type="file" onChange={(e) => setFile(e.target.files?.[0] ?? null)} /></Field><label style={{ display: "flex", gap: 8, alignItems: "center" }}><input type="checkbox" checked={form.requiresAction} onChange={(e) => setForm({ ...form, requiresAction: e.target.checked })} /> Requires action</label><label style={{ gridColumn: "1 / -1" }}>Notes<textarea className="thera-input" rows={4} value={form.notes} onChange={(e) => setForm({ ...form, notes: e.target.value })} /></label></div></WorkDrawer>}
  </>;
}
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label><div className="thera-field-label">{label}</div>{children}</label>; }
